"""UI layer — terminal rendering via the `rich` library.

Encapsulates all terminal output formatting (panels, tables, colours,
progress) so the game engine and agent layers remain output-agnostic.
To be populated in Stage 10.
"""